#BMEDIA

# YouTube Downloader – Netlify + HTML

A minimal web app that lets a user paste a YouTube link and receive a download link.  
Built with a static `index.html` and a Netlify Function (`download.js`) that runs `yt-dlp` on the server.

## Repo layout
